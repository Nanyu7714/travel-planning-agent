# 多城市个性化旅游规划平台 — UI 样式设计文档

| 项 | 内容 |
| :--- | :--- |
| 文档名称 | 多城市个性化旅游规划平台 UI 样式设计文档 |
| 版本 | V1.3 |
| 日期 | 2026-08-31 |
| 适用范围 | Vue 3 前端、用户端页面、Agent 页面和后台页面 |
| 设计方向 | 城市旅行杂志感 + 实用规划工具 |
| 目标设备 | 桌面端、平板、移动端 |

本文件规定前端的视觉、布局、交互和组件使用方式。页面实现应优先遵循本文，具体内容和接口以《旅游规划咨询Agent-开发文档.md》为准。

---

## 1. 设计定位

平台同时具有两种使用场景：

1. 用户浏览城市和热门排行时，需要有旅行内容产品的氛围。
2. 用户规划和调整行程时，需要有清晰、稳定和高效率的工具体验。

因此整体采用“轻内容、强信息、低装饰”的设计方式：使用真实城市图片表达目的地，用列表、时间线和数据组件承载信息，用克制的颜色表达热度和状态。

### 1.1 设计关键词

```text
清爽
可信
有城市感
便于扫描
适合连续操作
移动端可用
```

### 1.2 不采用的风格

- 不使用大面积紫色渐变。
- 不使用玻璃拟态、发光边框和悬浮球装饰。
- 不在页面中堆叠多层卡片。
- 不使用过大的营销型标题占据首屏。
- 不用模糊或过度压暗的图片表达景点。
- 不让所有按钮都使用胶囊形状。

---

## 2. 视觉基础规范

### 2.1 颜色系统（Airbnb 公共视觉基准）

当前网站公共壳层以 `DESIGN.md` 为视觉基准，并通过 `frontend/src/styles/airbnb.css` 应用 Airbnb 颜色、字体、圆角和阴影。下面的项目语义变量已按该基准映射；业务状态色仍需同时配合文字、图标或边框表达，不能只依赖颜色。

```css
:root {
  --color-bg: #ffffff;
  --color-surface: #ffffff;
  --color-surface-muted: #f7f7f7;
  --color-text: #222222;
  --color-text-secondary: #6a6a6a;
  --color-text-muted: #929292;
  --color-border: #ebebeb;
  --color-border-strong: #c1c1c1;

  --color-primary: #ff385c;
  --color-primary-hover: #e00b41;
  --color-primary-soft: #f7f7f7;

  --color-accent: #ff385c;
  --color-accent-soft: #ffd1da;
  --color-gold: #222222;
  --color-gold-soft: #f2f2f2;

  --color-success: #6a6a6a;
  --color-warning: #6a6a6a;
  --color-danger: #c13515;
  --color-info: #428bff;
}
```

颜色用途：

| 颜色 | 使用场景 |
| :--- | :--- |
| Rausch 粉红 | 主按钮、选中状态、主要链接、重要推荐标识 |
| 深色 Ink | 标题、正文、排名和主要数据 |
| Muted 灰 | 辅助文字、占位和非激活状态 |
| 浅灰 Surface | 标签、次级区域和白底上的轻分组 |
| 红色 | 删除、错误、不可满足的约束 |
| 蓝色 | 信息提示和地图相关辅助信息 |

颜色不能只依赖色彩表达状态，必须同时配合文字、图标或边框。

### 2.2 字体

中文优先使用系统字体：

```css
font-family:
  Inter,
  "PingFang SC",
  "Microsoft YaHei",
  "Noto Sans CJK SC",
  sans-serif;
```

字体层级：

| 用途 | 字号 | 字重 |
| :--- | ---: | ---: |
| 页面标题 | 28px | 600 |
| 区块标题 | 20px | 600 |
| 卡片标题 | 16px | 600 |
| 正文 | 14px | 400 |
| 辅助文字 | 13px | 400 |
| 数据数字 | 24～32px | 600 |
| 标签文字 | 12px | 500 |

不使用负字距，不根据视口宽度动态缩放字体。移动端标题通过换行适配，不强行缩小到难以阅读。

### 2.3 间距

```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
```

页面内容区使用 24～32px 间距，组件内部使用 12～16px 间距。普通页面桌面端最大内容宽度为 1200px；Agent 工作区允许扩大到 1440px。移动端两侧边距为 16px。

### 2.4 圆角和阴影

```css
--radius-sm: 4px;
--radius-md: 6px;
--radius-lg: 8px;
--shadow-panel: 0 1px 3px rgba(36, 49, 58, 0.08);
--shadow-popover: 0 8px 24px rgba(36, 49, 58, 0.12);
```

普通内容区域不使用阴影。阴影只用于下拉菜单、确认弹层、浮层和需要与背景分离的工具面板。

### 2.5 图标

使用 `lucide-vue-next`，图标默认 16～20px，线宽保持一致。

```text
搜索 Search
收藏 Bookmark
分享 Share2
返回 ArrowLeft
编辑 Pencil
删除 Trash2
刷新 RefreshCw
筛选 SlidersHorizontal
地图 Map
时间 Clock3
预算 WalletCards
```

图标按钮必须有 Tooltip。熟悉的操作可以只显示图标，不要把“刷新”“返回”“删除”等操作都做成带文字的圆角矩形按钮。

---

## 3. 全局布局

### 3.1 顶部导航

```text
┌──────────────────────────────────────────────────────────┐
│ 行旅    发现     热门排行     AI 规划     我的行程     登录 │
└──────────────────────────────────────────────────────────┘
```

规则：

- 顶部导航高度 64px。
- 内容最大宽度 1200px，居中显示。
- Logo 使用“行旅”文字标识，可配简单定位图标。
- 当前页面导航项使用青绿色文字和 2px 底部边框。
- 导航栏在滚动时保持顶部固定，但不覆盖页面内容。
- 移动端显示 Logo、页面标题和菜单图标。

### 3.2 内容区

```text
页面背景
  └── 最大宽度 1200px 的内容容器
        ├── 页面标题和操作区
        ├── 筛选或摘要区
        └── 内容列表 / 地图 / 对话 / 时间线
```

页面分区使用留白、标题和分割线，不将整页包在一个大卡片中。

### 3.3 页面状态

所有页面必须设计以下状态：

- 加载中：骨架屏或局部加载指示。
- 空数据：说明当前筛选条件下没有结果，并提供清除筛选操作。
- 加载失败：显示可理解的错误和重试按钮。
- 权限不足：显示登录或返回入口。
- 数据过期：显示数据更新时间，不伪装为实时数据。

---

## 4. 首页样式

首页定位为“旅行发现”，首屏直接提供城市和规划入口，不做纯营销落地页。

### 4.1 页面结构

```text
顶部导航
    ↓
季节推荐横幅
    ↓
热门城市排行
    ↓
热门景点排行
    ↓
AI 行程规划入口
```

### 4.2 季节推荐横幅

使用真实城市照片作为视觉主体，图片保持明亮、清晰和可辨识。文字放在图片空白区域或轻度遮罩区域，不放入独立浮动卡片。

内容示例：

```text
当季旅行目的地
看看这个季节适合去哪里
[开始规划]
```

横幅建议高度：

- 桌面端 320～380px。
- 移动端 260～320px。

### 4.3 首页排行

首页只展示前 3 名，使用紧凑的排行行项目：

```text
01  成都      美食 · 休闲       热度 92.4
02  上海      城市 · 文化       热度 89.8
03  北京      历史 · 博物馆     热度 87.6
```

每个排行区右上角提供“查看全部”文字链接，不将整个排行区制作成嵌套卡片。

---

## 5. 热门排行页面

### 5.1 页面结构

```text
热门排行
城市排行 | 景点排行

季度选择    城市选择    主题筛选

排行列表                         热度趋势图
```

### 5.2 筛选器

- 使用 Element Plus 的 Select、Segmented 或 Tabs。
- 筛选项必须有明确标签。
- 筛选变化后保留当前滚动位置。
- 移动端筛选器收进“筛选”按钮和底部抽屉。

### 5.3 排行列表

桌面端使用表格或紧凑列表，移动端使用纵向排行项。

排行项内容：

- 排名数字。
- 城市或景点缩略图。
- 名称和主要标签。
- 热度分数。
- 相对上一期的趋势。
- 查看详情按钮。

排行数字采用等宽数字，保证上下对齐。热度分数保留 1 位小数。

### 5.4 趋势图

- 使用 ECharts 折线图或柱状图。
- 只展示必要的 4～8 个数据点。
- 鼠标悬停显示具体周期和分数。
- 图表下方显示数据更新时间和统计口径。
- 无历史数据时显示“暂无趋势数据”，不能绘制虚假折线。

---

## 6. 城市和景点页面

### 6.1 城市详情页

结构：

```text
城市图片 + 城市名称 + 主要标签
城市简介 / 推荐季节 / 建议天数 / 预算参考
热门景点
推荐主题
[为我规划]
```

城市头图必须保留景点或城市环境的可识别信息，避免纯氛围图。城市标题在首屏可见，下面露出景点内容。

### 6.2 景点详情页

景点信息优先级：

1. 景点名称和图片。
2. 地址、开放时间、门票。
3. 推荐季节和标签。
4. 建议游玩时长。
5. 适合人群。
6. 地图位置。
7. “加入行程”或“用它规划”操作。

开放时间、门票和数据更新时间使用同一信息行展示，避免用户误以为数据实时有效。

“用它规划”始终跳转 Agent 页面，并把当前景点作为 `must_visit` 条件显示在需求摘要中。“加入行程”只在用户已有可编辑行程时显示；点击后先选择目标行程和日期，再进入重新规划状态，成功前不直接改变原时间线。未登录用户点击收藏或加入行程时跳转登录，并在登录后恢复原操作意图。

### 6.3 景点卡片

```text
┌──────────────────────────────────┐
│ 图片   景点名称                  │
│        历史文化 · 拍照 · 轻松     │
│        建议 2 小时  ·  门票免费   │
│        [查看详情]  [加入行程]     │
└──────────────────────────────────┘
```

图片比例固定为 4:3，避免图片加载导致列表跳动。卡片圆角 8px，边框优先于阴影。

### 4.4 热门城市推荐卡片

热门城市推荐卡片使用 `DESIGN.md` 的 Airbnb 视觉变量作为局部样例，不改变项目全局颜色主题。首页以三列推荐列表展示现有城市数据，组件仍可单独复用。

- 图片使用 4:3 比例，`object-fit: cover`，与用户需求保持一致。
- 卡片使用 `#ffffff` 背景、`#222222` 主文字和 14px 圆角；该 14px 取自 `DESIGN.md` 的 `property-card` 组件定义。
- 城市名称使用 16px、600、1.25 行高；景点数量标签使用 11px、600、1.18 行高。
- 默认不使用阴影；鼠标悬停时上浮 4px，并使用 `DESIGN.md` 的唯一卡片悬停阴影。
- 景点数量标签使用 `#f7f7f7` 背景、`#222222` 文字和全圆角，不额外引入新的颜色变量。
- 当前实现已接入首页热门城市推荐列表；景点数量由各城市现有景点数据实时统计，搜索结果沿用同一组卡片样式。
- 城市列表首次渲染使用 `TransitionGroup` 入场动画，从透明并下移 `10px` 过渡到正常位置；减少动画偏好下取消过渡。
- 城市卡片支持鼠标位置驱动的轻微倾斜，最大约 4deg，鼠标离开后回正；按钮按下、列表行 Hover 和键盘焦点均有状态反馈。

---

## 7. Agent 规划页面

Agent 页面是平台核心工作区，视觉重点是可读性、状态反馈和需求可控。

### 7.1 桌面端布局

```text
┌───────────┬────────────────────────────┬─────────────────┐
│ 会话列表  │ 对话区域                   │ 需求/行程侧栏   │
│ 220px     │ minmax(0, 1fr)             │ 280px           │
└───────────┴────────────────────────────┴─────────────────┘
```

Agent 工作区最大宽度 1440px，栏间距 16px。左侧会话列表可折叠，中间对话区最小宽度 0，内部正文最大阅读宽度 760px 并居中；右侧显示当前需求摘要，行程生成后切换为行程预览。视口小于 1280px 时优先折叠左侧会话列表，不能通过页面横向滚动容纳三栏。

### 7.2 对话气泡

- 用户消息靠右，使用浅青绿色背景。
- Agent 消息靠左，使用白色背景和细边框。
- 气泡最大宽度 78%。
- 长行程不塞进单个气泡，使用结构化行程卡。
- 时间、数据来源和状态文字使用 12px 辅助文字。

### 7.3 阶段状态

只显示用户需要理解的阶段摘要：

```text
正在整理旅行需求
正在查询景点
正在安排路线
正在检查时间和预算
```

不显示模型隐藏思维链，不使用“思考中……”无限旋转。请求超过设定时间时，显示真实失败原因和重试入口。

### 7.4 需求摘要栏

```text
旅行需求
出发地     杭州（城市比较时显示）
目的地     成都
天数       4 天
出发日期   2026-09-08
预算       ¥3000
估算范围   门票、景点间交通、餐饮、住宿
兴趣       美食、休闲
节奏       轻松
交通       公共交通

[修改需求]
```

已填写项使用深色文字，未填写项显示“待补充”。摘要栏不能只展示自然语言长段落，必须使用结构化字段。进入确认阶段后，出发城市、目的地、天数、人数、出发日期、预算、兴趣、排除地点、节奏和交通使用表单控件允许用户直接修改；确认后锁定控件。预算旁必须提供估算范围入口，不能使用“旅行总价”文案。行程详情页先以紧凑四列展示景点间交通，再以独立“往返跨城”区展示出发城市、目的地、距离、耗时和费用，避免两类交通混淆；预算明细单列往返跨城费用。移动端收为两列，避免数字拥挤。

### 7.5 确认卡

确认卡出现在对话流中：

```text
已整理你的旅行需求

成都 · 4 天 · 2 人
预算 ¥3000 · 美食、休闲 · 轻松节奏

[确认并生成行程] [修改]
```

主按钮使用青绿色，修改使用边框按钮。确认期间按钮必须有 loading、禁用和失败状态。

用户确认后保留当前窗口。生成成功时使用事件返回的准确行程 ID 自动进入行程工作台；生成失败时留在对话页并保留已填写清单，不跳转到历史行程或其他行程。

多候选城市时，确认卡之前展示城市对比表。每个城市使用一行或一个并列区块，统一展示匹配度、预算适配、交通便利度、适合原因和主要取舍，并提供“选择此城市”按钮。缺失数据使用“未计算”，不能显示为 0 分；用户选择前不展示最终行程。

### 7.6 行程结果卡

每一天独立展示，不在卡片中继续嵌套复杂卡片：

```text
Day 1  城市文化体验

09:30  景点 A       游玩 2 小时
       ↓ 公交 25 分钟
12:00  景点 B       游玩 2 小时

预计花费 ¥120       总时长 7.5 小时
```

操作：

- 重新规划：RefreshCw
- 增加景点：Plus
- 删除景点：Trash2
- 保存行程：Bookmark
- 查看地图：Map

行程生成后先显示“未保存草稿”状态。用户点击保存后切换为“已保存”，保存按钮进入禁用或已选中状态；网络重试不得在列表中产生重复行程。

---

## 8. 行程详情页

### 8.1 页面布局

```text
行程标题       成都 4 天游       [保存] [分享]
预算估算 ¥2800 4 天 · 2 人       V3 · 数据更新时间

[Day 1] [Day 2] [Day 3] [Day 4]

左侧时间线                         右侧地图
```

桌面端地图宽度约占 42%，时间线约占 58%。移动端先展示时间线，地图放在时间线之后或使用“查看地图”抽屉。

### 8.2 约束提示

```text
✓ 开放时间匹配
✓ 每日安排适中
✓ 预计预算未超出
```

约束满足使用绿色图标和文字；无法满足时使用红色警告，并明确说明冲突来源，不只显示“规划失败”。

### 8.3 编辑交互

- 用户可以拖动或使用上下移动按钮调整顺序。
- 删除景点需要确认，但不使用复杂弹窗堆叠。
- 修改预算、节奏和兴趣后，使用 Agent 重新规划。
- 每次修改后显示“已重新计算”的时间和算法版本。
- 调整期间保留旧版本并显示任务状态；成功后提示“已生成 V4”，允许查看 V3。版本记录抽屉为历史已保存版本提供“恢复为新版本”，不直接改写旧版本。
- 服务端返回版本冲突时，禁止覆盖，显示“行程已在其他页面更新”，提供重新载入和基于最新版再次修改操作。
- 拖动排序必须支持键盘的上移、下移操作，移动端提供显式排序按钮作为替代。

分享操作使用弹层显示只读链接、分享版本、失效时间、复制和撤销按钮。生成失败时保留当前页面；撤销后明确显示链接已失效。公开分享页不显示账号、会话、编辑、收藏和重新规划入口，只展示创建链接时固定的已保存版本、数据来源和预算范围；原行程产生新版本时提示用户已有链接不会自动更新。

---

## 9. 用户中心和后台

### 9.1 用户中心

用户中心保持简单：

```text
我的行程
我的收藏
最近浏览
个人资料
账号安全
```

行程列表使用横向信息项，显示城市、天数、当前版本、更新时间和预算，不使用过度装饰的仪表盘。历史版本放在行程详情的“版本记录”抽屉中，不与当前行程混在主列表。

收藏页使用“城市 / 景点”分段控件，支持取消收藏和直接进入详情；最近浏览按最后查看时间排序，提供清空操作；个人资料用于修改昵称，账号安全包含邮箱、密码、登录设备、退出和删除账号。删除账号必须二次确认并重新验证密码，不能使用普通 Toast 代替风险说明。

### 9.2 后台管理

后台可以使用更密集的管理界面，但沿用颜色、字体和按钮规范。

主要组件：

- 表格：城市、景点、排行数据。
- 筛选栏：城市、状态、更新时间。
- 抽屉：编辑景点信息。
- 上传区：图片和 JSON/CSV 数据。
- 操作日志：导入、更新、删除和排行生成结果。

后台不使用与用户端完全不同的视觉主题，避免维护两套设计系统。

后台路由包含 `/admin`、`/admin/cities`、`/admin/attractions`、`/admin/rankings`、`/admin/imports`、`/admin/planning-jobs`、`/admin/feedback`、`/admin/users` 和 `/admin/audit-logs`。导入数据必须依次显示“校验中、可确认、导入中、成功、失败”状态；校验失败提供可下载的行号和错误摘要，不允许只显示“上传失败”。普通用户进入后台路由时显示 403 页面，不短暂渲染后台数据。

后台首页使用数据摘要条和密集列表：显示内容数量、规划任务状态、数据更新时间、排行生成状态和待处理反馈。城市、景点和用户页面采用筛选栏加表格；编辑操作使用右侧抽屉，危险操作使用明确的确认对话框。后台不展示密码、Token、完整私人对话或未经脱敏的 IP。

排行发布使用“生成草稿 → 预览分项分数 → 发布”三步状态。数据导入使用“上传 → 校验 → 预览 → 确认 → 结果”步骤条，失败结果必须提供错误行号。规划任务页面允许查看阶段、耗时和错误码，只对可重试错误显示一次“重试”操作。

### 9.3 登录、注册和账号安全

登录和注册页使用单列窄表单，桌面端宽度 360～420px，移动端占满内容宽度。注册字段依次为邮箱、昵称、密码、确认密码和隐私规则确认；登录只显示邮箱、密码、登录按钮和找回密码入口。允许浏览器自动填充和密码管理器，不阻止粘贴密码。

错误显示在对应字段附近，登录失败统一使用“邮箱或密码不正确”，不能通过文案泄露账号是否存在。提交期间按钮进入 loading 并防止重复点击；注册表单提交后保留当前输入，直到用户切换认证方式或关闭页面，方便发现邮箱填错时直接更正；页面不得回显、记录或发送密码到注册请求之外的位置。

验证邮箱页面包含邮箱、6 位数字验证码、发送验证码按钮和验证按钮。发送后按钮显示 60 秒倒计时，验证码 3 分钟失效；页面覆盖发送中、发送成功、验证码错误或过期、验证成功状态，成功后提供登录入口。找回密码无论邮箱是否存在都显示相同结果。账号安全页分别管理邮箱、密码、登录设备和注销账号，不把这些操作塞进同一个确认弹窗。撤销设备和退出全部设备必须显示作用范围。

登录后导航根据用户身份显示不同入口：普通用户显示“我的行程”和“账号设置”，管理员额外显示“管理后台”。管理员进入后台后左侧导航保持固定，顶部显示当前管理员身份和退出入口；不能仅通过隐藏菜单来代替权限控制。

---

## 10. 响应式规范

### 10.1 断点

```css
--bp-mobile: 767px;
--bp-tablet: 1023px;
--bp-desktop: 1024px;
```

### 10.2 移动端规则

- 顶部导航折叠菜单。
- 多列内容改为单列。
- Agent 右侧摘要改为顶部折叠区域。
- 行程地图改为底部抽屉或独立区域。
- 表格改为排行项或横向滚动区域。
- 操作按钮保证最小触控区域约 44px。
- 页面两侧保留 16px 安全边距。
- 长标题、城市名和标签必须允许换行。

### 10.3 平板规则

- 隐藏或折叠左侧会话列表。
- 1024～1279px 宽度下，Agent 右侧摘要允许切换为抽屉，确保对话区不小于 640px。
- 行程页采用上下布局。
- 首页排行区从三列改为两列。

---

## 11. 交互和状态规范

### 11.1 按钮

```text
primary       青绿色实心，提交、确认、生成
secondary     白底边框，修改、查看详情
text          无边框，取消、查看全部
danger        红色，仅删除和不可逆操作
icon          纯图标，返回、刷新、关闭、收藏
```

按钮必须包含 hover、active、focus、disabled 和 loading 状态。提交后防止重复点击。

### 11.2 标签

- 城市标签：青绿色浅底。
- 兴趣标签：灰绿色浅底。
- 热门标签：珊瑚橙浅底。
- 季节标签：金黄色浅底。
- 错误标签：浅红底。

标签内容过长时换行，不强制单行溢出。

### 11.3 加载

- 列表加载使用骨架屏。
- Agent 生成使用阶段状态。
- 地图加载使用局部占位，不遮挡整个页面。
- 生成行程期间保留需求摘要，避免用户失去上下文。

### 11.4 通知

Toast 只用于短反馈：保存成功、复制成功、请求失败。重要错误必须在对应组件附近同时显示，不只依赖 Toast。

---

## 12. 图片和数据可视化

### 12.1 图片

- 景点主图使用真实景点或城市图片。
- 城市和景点详情页不得使用 AI 图片冒充真实景点。
- AI 生成图片只用于首页季节横幅、专题封面、空状态和装饰性场景。
- 使用远程图片时，必须同时维护图片来源、作者、许可证和原始链接。
- 需要署名的图片在图片下方或详情区域提供作者和许可证入口。
- 不直接使用搜索引擎结果或旅游网站中未确认授权的图片。
- 所有图片使用固定宽高比。
- 图片加载失败显示统一占位图和景点名称。
- 使用 `loading="lazy"` 加载非首屏图片。
- 不把关键文字放在图片边缘。
- 图片必须有可读的 `alt` 文本。

图片元数据示例：

```json
{
  "image_url": "https://...",
  "image_source": "Wikimedia Commons",
  "image_author": "作者名称",
  "image_license": "CC BY-SA 4.0",
  "attribution_url": "https://..."
}
```

MVP 可以先使用合法的远程图片地址；图片数量增加后，将获得授权的图片上传到阿里云 OSS，通过 OSS 或 CDN 地址展示，避免大量占用应用服务器磁盘。

### 12.2 图表

ECharts 只用于有数据关系需要表达的地方：

- 季度热度趋势。
- 热门城市分数构成。
- 用户行程预算构成。

图表必须配合文字摘要，不能要求用户只通过颜色和图形理解数据。

### 12.3 地图

- 地图用于表达位置和路线，不承担全部文字信息。
- 每个标记点需要对应景点名称。
- 路线颜色使用主色，已完成路线和待规划路线要有区别。
- 路线 API 失败时显示文字路线和失败原因。
- 地图数据缺失时保留景点时间线，不绘制猜测路线。
- 前端只使用受生产域名限制的高德 JavaScript API Key，不得包含后端 Web Service Key。

---

## 13. 前端实现规范

### 13.1 推荐组件目录

```text
src/
├── components/
│   ├── layout/
│   ├── city/
│   ├── attraction/
│   ├── ranking/
│   ├── agent/
│   ├── itinerary/
│   └── common/
├── views/
├── styles/
│   ├── tokens.css
│   ├── global.css
│   └── utilities.css
└── composables/
```

### 13.2 核心组件

```text
AppHeader.vue
CityHero.vue
RankingList.vue
RankingItem.vue
AttractionCard.vue
FilterBar.vue
AgentChat.vue
AgentStage.vue
RequirementSummary.vue
PlanConfirmCard.vue
ItineraryTimeline.vue
ItineraryMap.vue
ItineraryVersionDrawer.vue
ItineraryShareDialog.vue
ConstraintStatus.vue
FavoriteList.vue
RecentViewList.vue
ImportBatchStatus.vue
AuthForm.vue
AuthActionState.vue
SessionDeviceList.vue
EmptyState.vue
ErrorState.vue
```

### 13.3 CSS 原则

- 颜色、间距、圆角和阴影必须使用 CSS 变量。
- 不在组件中散落大量硬编码颜色。
- 不通过 `transform: scale` 造成布局跳动。
- 图片、地图、时间线和按钮使用稳定尺寸。
- 内容超长时换行或截断，并提供完整内容查看方式。
- 页面级布局使用 CSS Grid/Flex，避免依赖大量绝对定位。

### 13.4 Element Plus 使用范围

适合使用 Element Plus：

```text
Form
Input
Select
Tabs
Table
Drawer
Dialog
Tag
Tooltip
Pagination
Skeleton
Message
```

首页、Agent 对话、行程时间线和景点内容卡使用自定义组件，不直接堆叠默认 Element Plus Card，避免页面呈现为模板化后台。

---

## 14. 可访问性和兼容性

- 所有表单输入必须有 label 或可读的 aria-label。
- 图标按钮必须有 Tooltip 或 aria-label。
- 键盘可以完成导航、筛选、发送和确认操作。
- Focus 状态不能被 CSS 去掉。
- 正文和背景颜色满足基本对比度要求。
- 图片和地图信息提供文字替代。
- 不把 hover 作为唯一的信息展示方式。
- 在 375px 宽度下不出现页面级横向滚动。
- 在 1440px 宽度下内容不无限拉伸。

---

## 15. 页面验收清单

### 全局

- [ ] 导航在桌面端和移动端都可用。
- [ ] 页面最大宽度和两侧边距统一。
- [ ] 所有主要操作具有 loading、disabled 和错误状态。
- [ ] 没有无意义的渐变、发光和装饰性圆形元素。

### 首页和排行

- [ ] 首屏可以直接进入城市浏览或 AI 规划。
- [ ] 热门排行显示周期和数据更新时间。
- [ ] 排行列表在移动端可正常阅读。
- [ ] 无历史趋势时不绘制虚假图表。

### Agent

- [ ] 用户消息、Agent 消息和阶段状态易于区分。
- [x] 需求摘要使用结构化字段展示，并支持确认前编辑结构化条件。
- [x] 规划前有确认卡，生成成功后按准确行程 ID 自动进入工作台。
- [ ] 不展示模型隐藏思维链。
- [ ] 生成失败时保留用户输入和已提取需求。
- [ ] 多候选城市的数据缺失项明确显示“未计算”，用户可以自主选择城市。
- [ ] 任务排队、执行、取消和失败状态均有对应反馈。

### 行程

- [ ] 每日行程有清晰时间线。
- [ ] 地图和文字路线可以互相对应。
- [ ] 预算、时间和开放状态可见。
- [ ] 预算明确标注估算范围，草稿和已保存状态可以区分。
- [ ] 调整行程时旧版本保持可见，成功、失败、取消和版本冲突都有明确状态。
- [ ] 分享链接可以创建、复制和撤销，公开页不泄露用户身份或编辑入口。
- [ ] 移动端不出现时间线和地图重叠。

### 用户中心和后台

- [ ] 收藏、最近浏览、账号设置与登录恢复操作完整。
- [x] 用户主页集中展示偏好、排除地点、收藏和最近浏览；账号安全页展示设备并支持逐台退出或全部退出。
- [x] 行程工作台提供增删天数、增删/替换/排序景点、时间编辑、版本恢复、分享、五颗半星评分和评论入口。
- [ ] 登录、注册、邮箱验证和密码重置覆盖加载、成功、通用失败和令牌过期状态。
- [ ] 表单支持浏览器自动填充和密码管理器，错误不会泄露账号是否存在。
- [ ] 删除账号前重新验证密码，并清楚说明数据处理结果。
- [ ] 后台导入展示校验、确认、执行和错误明细。
- [ ] 普通用户无法看到后台内容，管理员写操作有完成或失败反馈。

### 数据和安全

- [ ] 图片加载失败有占位状态。
- [ ] 数据来源和更新时间可查看。
- [ ] 前端代码中没有大模型密钥、高德 Web Service Key、数据库密码或 JWT 密钥。
- [ ] 前端高德 JavaScript API Key 已限制生产域名，并与后端 Key 分离。
- [ ] 用户只能查看和修改自己的行程。

---

## 16. 后续修订记录

| 版本 | 日期 | 修订内容 |
| :--- | :--- | :--- |
| V1.3 | 2026-08-31 | 补充用户端账号流程、管理员后台页面和模块化前端结构约定 |
| V1.2 | 2026-08-31 | 补齐加入行程、修订版本、分享、用户中心、认证和后台导入交互 |
| V1.1 | 2026-08-31 | 修正 Agent 工作区宽度，补充城市比较、预算范围、任务状态和地图密钥规范 |
| V1.0 | 2026-08-31 | 建立初版视觉、布局、组件和响应式规范 |

---

## 17. V2.0 全站视觉与布局重设计方案（待实施）

### 17.1 修改原因

本次重设计的目标不是单独调整热门城市卡片，而是统一重做网站的页面布局、部件尺寸、视觉变量和交互状态。现有项目的页面已经覆盖首页、城市、景点、排行、Agent、行程、用户中心和后台，但不同页面仍存在布局密度、颜色、圆角、阴影和按钮规格不统一的问题。

现有项目样式文档曾使用青绿色作为公共主色，而 `DESIGN.md` 明确规定 Airbnb Rausch 为主色，并定义了完整的颜色、字体、圆角和阴影变量。由于本次需求要求依据 `DESIGN.md` 重新设计整站，V2.0 将以 `DESIGN.md` 作为视觉基准；现有开发文档仍作为功能范围、路由和业务交互的依据。旧版本内容保留作为历史记录，不直接覆盖。

本节记录的是设计决策和实施方案，不代表相关代码已经完成。

### 17.2 总体设计方向

平台采用两种相互统一的体验：

- 城市发现使用旅行内容产品的布局，图片优先，信息简洁，保留足够留白。
- 行程规划使用工作台布局，信息密集，结构稳定，适合连续编辑和确认。

整体不使用大面积渐变、玻璃拟态、发光边框、装饰性悬浮球或营销型超大标题。页面通过真实城市图片、分组标题、列表、时间线和分割线建立层级。

### 17.3 全站视觉基准

`DESIGN.md` 中的变量作为公共视觉基准：

```css
:root {
  --color-primary: #ff385c;
  --color-primary-active: #e00b41;
  --color-primary-disabled: #ffd1da;
  --color-error: #c13515;
  --color-ink: #222222;
  --color-body: #3f3f3f;
  --color-muted: #6a6a6a;
  --color-muted-soft: #929292;
  --color-border: #dddddd;
  --color-border-soft: #ebebeb;
  --color-border-strong: #c1c1c1;
  --color-canvas: #ffffff;
  --color-surface-soft: #f7f7f7;
  --color-surface-strong: #f2f2f2;
  --radius-card: 14px;
  --radius-control: 8px;
  --radius-pill: 9999px;
  --shadow-hover: rgba(0, 0, 0, 0.02) 0 0 0 1px,
    rgba(0, 0, 0, 0.04) 0 2px 6px 0,
    rgba(0, 0, 0, 0.1) 0 4px 8px 0;
}
```

字体统一使用：

```css
font-family: 'Airbnb Cereal VF', Circular, -apple-system, system-ui,
  Roboto, 'Helvetica Neue', sans-serif;
```

中文环境缺少 Airbnb Cereal VF 时，使用项目已有的中文系统字体回退。页面标题使用 `28px / 700`，区块标题使用 `21px / 700`，卡片标题使用 `16px / 600`，正文使用 `16px / 400`，卡片辅助文字使用 `14px / 400`，标签使用 `11px / 600`。不使用负字距，也不根据视口宽度缩放字号。

间距使用 `DESIGN.md` 的 2px、4px、8px、12px、16px、24px、32px、48px、64px 阶梯。普通卡片间距为 `16px`，卡片内部间距为 `16px`，大型工具面板内部间距为 `24px`，主要页面区块上下间距为 `64px`。

普通内容默认不使用阴影。卡片悬停、下拉菜单和浮层只使用上述唯一的 `--shadow-hover`，不新增多级阴影体系。

### 17.4 全局页面框架

顶部导航统一为 `80px` 高，内容最大宽度为 `1280px`，桌面端左右内边距为 `32px`：

```text
Logo       发现   热门排行   AI 规划   我的行程       登录 / 用户菜单
```

导航规则：

- 白色背景，底部使用 `1px` 细边框。
- 当前页面使用 `#222222` 文字和底部高亮线。
- Logo 使用文字标识，可配 `lucide-vue-next` 定位图标。
- 移动端保留 Logo、当前页面标题和菜单按钮，导航项进入抽屉或下拉面板。

普通页面内容最大宽度为 `1280px`，Agent 工作区最大宽度为 `1440px`。页面不使用包住整页的大卡片，页面分区通过标题、留白和分割线区分。

### 17.5 公共部件尺寸

先统一公共部件，再逐页替换旧页面样式：

```text
TopNav
PageHeader
PrimaryButton
SecondaryButton
IconButton
TextInput
FilterTabs
StatusBadge
Modal
EmptyState
LoadingState
PopularCityCard
AttractionCard
RankingRow
ItineraryDay
TimelineStop
```

尺寸基准：

| 部件 | 尺寸和状态 |
| :--- | :--- |
| 主按钮 | 高度 `48px`，内边距 `14px 24px`，圆角 `8px` |
| 次按钮 | 高度不低于 `48px`，白色背景，`1px` 边框 |
| 输入框 | 高度 `56px`，圆角 `8px`，内边距 `14px 12px` |
| 图标按钮 | `40px` 或 `48px`，圆形按钮使用全圆角 |
| 标签 | `4px 10px` 内边距，圆角 `9999px` |
| 普通卡片 | 白色背景，圆角 `14px`，内部间距 `16px` |
| 工具面板 | 白色背景，圆角 `14px`，内部间距 `24px` |
| 卡片间距 | `16px` |

所有图标按钮必须提供 `aria-label`，不熟悉的图标补充 Tooltip。按钮、输入框、列表项必须保留键盘焦点状态，并在 `prefers-reduced-motion` 下关闭主要动画。

### 17.6 首页

首页定位为“旅行发现”，首屏直接提供城市浏览和 AI 规划入口：

```text
顶部导航
↓
季节推荐横幅
↓
热门城市推荐
↓
热门景点排行
↓
AI 行程规划入口
```

季节推荐横幅使用清晰、明亮、可辨识的真实城市图片。桌面端高度为 `320px ~ 380px`，移动端为 `260px ~ 320px`。文字放在图片空白区域或轻度遮罩区域，不放进额外浮动卡片。

热门城市推荐使用 `PopularCityCard`：

- 桌面端 `4` 列，平板 `2 ~ 3` 列，移动端 `1` 列。
- 图片固定为 `4:3`，使用 `object-fit: cover`，防止图片加载造成布局跳动。
- 卡片使用 `#ffffff` 背景、`#222222` 文字和 `14px` 圆角。
- 城市名称使用 `16px / 600 / 1.25`。
- 景点数量使用 `11px / 600 / 1.18` 胶囊标签。
- 默认无阴影，桌面鼠标悬停时上浮 `2px ~ 4px` 并使用 `--shadow-hover`。

热门景点排行使用紧凑列表，不再把整个排行区包成大卡片：

```text
01   景点名称          成都 · 历史文化       热度 92.4
02   景点名称          上海 · 城市漫游       热度 89.8
03   景点名称          北京 · 博物馆         热度 87.6
```

### 17.7 城市和景点页面

城市列表页采用“页面标题 → 搜索和筛选 → 城市网格”的顺序。城市详情页采用：

```text
城市头图
城市名称和标签
城市简介
推荐季节 / 建议天数 / 预算参考
热门景点
推荐主题
开始规划
```

城市头图桌面端高度约 `360px`，移动端约 `260px`。标题必须在首屏可见，首屏底部露出后续景点内容。

景点卡片使用 `4:3` 图片。桌面端采用左图右文，图片宽度约 `120px ~ 150px`；移动端改为上下结构。信息包含景点名称、标签、开放时间、价格和详情入口。优先使用边框分隔，不使用厚重阴影。

景点详情页桌面端使用左右两列，左侧图片约 `42%`，右侧信息约 `58%`；移动端改为单列。页面首要操作为“用它规划”，同时清晰展示收藏状态、开放时间、门票、建议时长和数据更新时间。

### 17.8 热门排行页

排行页布局为：

```text
页面标题
城市排行 | 景点排行
筛选工具栏
排行列表                         趋势图
```

桌面端排行列表约占 `60%`，趋势图约占 `40%`。排行项高度约 `72px`，缩略图约 `48px × 48px`，显示排名、名称、标签、热度分数和环比趋势。

移动端趋势图放在列表之后，筛选项收进“筛选”按钮和底部抽屉。没有历史数据时显示“暂无趋势数据”，不绘制虚假折线。

### 17.9 Agent 规划工作台

桌面端使用三栏工作区，最大宽度为 `1440px`：

```text
会话列表 220px | 对话区域 minmax(0, 1fr) | 需求摘要 280px
```

中间对话正文最大阅读宽度为 `760px`。用户消息靠右，Agent 消息靠左；复杂行程使用结构化行程结果，不塞进单个气泡。需求摘要栏使用字段列表展示目的地、日期、天数、人数、预算、兴趣、节奏和交通方式，并提供“修改需求”。

视口小于 `1280px` 时优先折叠左侧会话列表；小于 `744px` 时改为单栏，需求摘要变成顶部摘要条或底部抽屉，输入区固定在底部，主要操作按钮高度不低于 `48px`。任何断点都不允许通过页面横向滚动容纳三栏。

### 17.10 行程详情页

桌面端布局为：

```text
行程标题和操作
预算摘要
Day 标签栏

时间线 58%                地图 42%
```

移动端先展示时间线，地图放到“查看地图”按钮或抽屉中。每个行程点显示时间、景点、游玩时长、交通方式和预计花费，并提供编辑、删除、上移和下移操作。每天独立展示，不在单个卡片内继续嵌套大量卡片。

草稿和已保存状态必须区分；重新规划、版本恢复、分享、网络失败和版本冲突都要有明确状态反馈。

### 17.11 用户中心和后台

用户中心保持列表型布局：

```text
我的行程
我的收藏
最近浏览
个人资料
账号安全
```

行程列表每行显示城市、天数、当前版本、更新时间、预算和查看入口。用户中心桌面端最大宽度约 `1000px`，移动端使用单列。

后台使用更高的信息密度，但沿用同一套颜色、字体和按钮规范。页面基本结构为“标题 → 筛选工具栏 → 表格或列表 → 抽屉编辑”。表格行高建议 `64px ~ 72px`，危险操作使用明确确认对话框，数据导入使用“上传 → 校验 → 预览 → 确认 → 结果”步骤条。

### 17.12 响应式规则

```text
Mobile:  < 744px
Tablet:  744px - 1128px
Desktop: 1128px - 1440px
Wide:    > 1440px
```

响应式调整优先减少列数，不把内容压缩到不可读：

```text
城市卡片：4 列 → 3 列 → 2 列 → 1 列
Agent：三栏 → 两栏 → 单栏
景点卡片：左右结构 → 上下结构
排行趋势图：并列展示 → 列表下方展示
```

在 `375px` 宽度下不出现页面级横向滚动，在 `1440px` 宽度下内容不无限拉伸。图片、按钮、网格和时间线使用稳定尺寸，避免加载或 Hover 导致布局跳动。

### 17.13 实施顺序

1. 新建全局颜色、字体、间距、圆角和阴影变量。
2. 重做顶部导航、页面容器和全局响应式框架。
3. 重做按钮、输入框、标签、图标按钮、浮层、空状态和加载状态。
4. 重做首页和 `PopularCityCard`。
5. 重做城市列表、城市详情和景点详情。
6. 重做热门排行页面。
7. 重做 Agent 规划工作台和行程详情页。
8. 重做用户中心、认证页面和后台页面。
9. 逐页验证桌面端、平板端、移动端、键盘焦点、图片失败、空数据、加载失败和 Hover 状态。

### 17.14 版本记录

| 版本 | 日期 | 修订内容 |
| :--- | :--- | :--- |
| V2.0 方案 | 2026-09-02 | 重新规划全站页面布局、组件尺寸、Airbnb 视觉变量、响应式规则和实施顺序；保留 V1.x 作为历史设计记录 |

### 17.15 第一阶段实现状态

2026-09-02 已开始实施 V2.0，当前完成范围如下：

- `frontend/src/styles/airbnb.css` 已补齐 `DESIGN.md` 的 Airbnb 颜色、文字、表面、边框、圆角和唯一悬停阴影变量，同时保留旧语义变量作为现有页面的兼容映射。
- 全局顶部导航和页面容器已应用 `80px` 导航高度、`1280px` 普通页面最大宽度、`32px` 桌面端内边距和 Airbnb 字体层级覆盖。
- 首页已改为“真实城市图片首屏 → 热门城市 → 景点发现与热度排行 → AI 规划入口”的结构。
- 首页热门城市已统一使用 `PopularCityCard`，图片固定 `4:3`，卡片使用 `14px` 圆角，Hover 使用轻微上浮和 `DESIGN.md` 悬停阴影。
- 首页支持城市搜索为空时恢复完整城市列表，并为首屏图片提供加载失败占位。

以下内容仍待后续阶段实施：城市详情、景点详情、排行页、Agent 工作台、行程详情、用户中心和后台页面的逐页布局重做；本阶段不将这些页面标记为已完成。

### 17.16 第二阶段实现状态

2026-09-02 已完成城市浏览链路的第二阶段实施：

- `CityCard` 已移除 3D 倾斜，统一为 `14px` 圆角、`4:3` 图片、全局 Airbnb 变量和轻微 Hover 上浮。
- 城市列表页增加城市搜索、全部城市 / 可规划 / 即将开放筛选和无结果状态；卡片网格在桌面、平板和移动端逐级减少列数。
- 城市详情页使用城市真实图片，增加“为我规划”入口，景点推荐改为带图片的 `4:3` 卡片网格。
- 景点详情页使用景点真实图片，增加“用它规划”入口，开放时间、建议时长、门票、标签和收藏状态按信息优先级排列。
- 热门排行页增加城市排行 / 景点排行切换、景点城市筛选、空数据状态和数据说明区域；没有历史快照时明确不绘制趋势图。

以下内容仍待后续阶段实施：Agent 工作台、行程详情、用户中心和后台页面的逐页布局重做。

### 17.17 第三、四阶段实现状态

2026-09-02 已完成 V2.0 核心工作区和账户管理页面的第一轮落地：

- Agent 工作台已按三栏结构应用 `220px / minmax(0, 1fr) / 280px`，中间对话正文限制在约 `760px`，右侧需求摘要使用结构化表单。
- 会话列表在工作区中保持可折叠；视口缩小时隐藏左栏，移动端改为单栏并把需求摘要放到对话之后。
- 我的行程列表使用左侧行程选择、右侧预算和时间线的工作区布局；移动端改为可横向浏览的行程选择项。
- 行程详情页统一编辑、历史版本、自然语言调整、时间线、评分和分享区域的边框、圆角、输入高度和间距。
- 用户中心、个人资料和账号安全页已统一表单高度、列表行高、分段标题和危险操作样式。
- 后台首页和后台数据表已统一使用紧凑工具栏、`64px ~ 72px` 表格行、状态胶囊和规范悬停阴影。

截至本次记录，V2.0 的主要页面布局和公共视觉基座已经完成第一轮覆盖；后续主要工作转为逐页视觉验收、真实图片与地图接入、功能缺口修复和细节优化。

### 17.18 全局导航细节实现状态

2026-09-02 已完成全局导航、账户菜单和登录注册弹窗的第一轮实现：

- 桌面端顶部导航保持 `80px` 高，移动端在小于 `744px` 时切换为 `64px` 高，并使用 `40px` 圆形菜单按钮。
- 移动端导航项进入顶部下拉面板，菜单项保持 `48px` 高，当前页面使用浅灰背景和文字高亮。
- 登录用户使用 `40px` 高的胶囊账户入口，账户菜单包含用户主页、账号安全和退出登录，并使用 `14px` 圆角和规范悬停阴影。
- 登录 / 注册弹窗使用 `440px` 最大宽度、`32px` 桌面内边距、`24px` 移动内边距；认证方式使用带底部指示线的分段标签。
- 弹窗输入框使用 `56px` 高、`8px` 圆角，主提交按钮保持 `48px` 高。
- 支持点击遮罩关闭、按 `Escape` 关闭菜单或弹窗、打开弹窗后焦点落到关闭按钮；提交期间按钮进入禁用状态，防止重复提交。
- 弹窗遮罩使用 `#000000` 的 `50%` 透明度，符合 `DESIGN.md` 的 Scrim 规范。

### 17.19 Agent 规划依据面板

2026-09-02 在行程详情页增加只读“规划依据”面板：

- 面板位于自然语言调整区之后、行程时间线之前，展示候选景点数量、最终选择数量和预算修复提示。
- 工具调用以编号列表呈现，只展示“检索景点资料、筛选候选景点、校验行程约束”等用户可理解的步骤，不暴露内部提示词或原始数据库内容。
- 面板沿用行程详情页的白色表面、`--color-border-soft` 边框、`--radius-card` 圆角、`22px` 内边距；移动端圆角与其他详情面板统一为 `--radius-control`。

### 17.20 图片管理页面

2026-09-02 在管理后台新增图片管理页面：

- 使用紧凑表格展示缩略图、归属、来源与授权、核验状态和操作；图片缺失时使用图标占位。
- 筛选区使用城市、核验状态和关键词三个控件，避免管理员在大量图片中逐条查找。
- 编辑区作为表格后的独立工具面板，左侧固定预览，右侧为两列元数据表单；移动端改为单列，避免输入框拥挤。
- “自动找图”是明确命令按钮，编辑使用带提示的图标按钮；启用状态使用复选框，只有已核验图片可保存为启用状态。

### 17.21 会话菜单边界行为

2026-09-02 修复会话列表的更多操作菜单：点击操作按钮时，根据按钮到视口底部的可用空间决定展开方向。底部空间不足以容纳菜单时，菜单以按钮为锚点向上展开；其余位置维持向下展开，避免菜单被页面底栏或会话滚动容器裁切。

### 17.22 后台内容编辑面板

2026-09-02 在城市、景点和排行管理页使用表格上方的独立编辑面板：表格负责扫描已有资料，编辑面板负责新增、修改和批量导入，避免在表格单元格内塞入大量输入控件。面板字段在桌面端使用两列网格，移动端变为单列。城市和景点的上架状态使用眼睛图标按钮切换；已上架内容不显示彻底删除入口，停用后才显示垃圾桶按钮。停用和彻底删除分别确认，彻底删除提示不可撤销及关联保护，避免与普通保存混为同一动作。

### 17.23 首页城市探索区

2026-09-02 将热门城市推荐之后的首页内容重组为以当前城市为中心的探索区：

- 左侧使用城市简介、出行信息和最多 4 张景点图片卡片。景点卡保持 `4:3` 图片比例、`--radius-card` 圆角与 `16px` 间距，悬停仅使用 `translateY(-3px)` 与 `--shadow-hover`。
- 右侧使用无浮层卡片的紧凑热度趋势栏，按排名、名称、城市、热度数值和比例条展示前 5 个景点；桌面端通过左侧分割线与景点区分组。
- 数据为空时，探索区和趋势栏分别显示明确的整理状态，避免页面留下无内容的大块留白。
- 底部规划入口改为三列工具条，主按钮自动携带当前城市的 `slug`；移动端按“图标与文案、全宽按钮”两行排列。

### 17.24 首页首屏图片鼠标切换

2026-09-02 首页首屏横幅从已有城市资料中选择最多 5 张已配置图片。桌面端鼠标在横幅内横向移动时，按当前位置分区切换对应城市图片；图片不会在每一个像素事件重复切换。切换采用 `420ms` 的透明度、`14px` 虚化与轻微缩放交叉过渡，新图由虚化变清晰。触屏指针不触发此交互；用户系统开启“减少动态效果”时取消过渡动画。加载失败的图片会自动从本次首屏图片组移除，所有图片不可用时显示既有占位状态。

2026-09-02 当前首屏改为使用 `frontend/public/hero` 中的 6 张本地城市图片（北京、成都、广州、南京、上海、长沙），页面挂载时预加载。鼠标横向移动将横幅划分为 6 个切换区，图片在区间边界切换，以避免每次微小移动都刷新视觉状态。后续替换为热度城市图片时，保持该图片对象结构和过渡规则，仅将本地列表换为基于城市热度接口的图片数据。

### 17.25 首页城市选择与规划入口

2026-09-02 热门城市推荐轨道中的卡片在首页承担“选择当前城市”的职责：鼠标悬停、点击或键盘聚焦都会刷新下方的城市简介、景点卡和规划入口，不再直接跳转城市详情。当前城市卡使用 `2px` 主色内描边标识；城市详情由探索区的“查看城市指南”文字链接进入。规划入口置于首屏横幅之后、热门城市轨道之前，桌面端保持图标、文案、按钮三列布局，移动端按钮扩展为全宽。

### 17.26 登录注册入口页

2026-09-02 根路径 `/` 使用独立登录注册入口页，业务首页移动到 `/discover`。入口页隐藏常规顶部导航和页脚，采用真实城市图片作为全屏旅行背景：鼠标在背景内移动时按横向位置切换 6 张本地城市图片，并根据鼠标坐标进行轻微景深位移；换景使用 `520ms` 虚化、透明度和缩放过渡。表单使用白色 `14px` 圆角工具面板，输入框为 `56px` 高、`8px` 圆角，主操作使用 Rausch `#ff385c`，游客浏览使用深色描边次要按钮。小于 `744px` 时，背景保留、文案与表单收为单列；系统开启减少动态效果时关闭背景动画。

### 17.27 行程回收站

2026-09-02 管理后台新增行程回收站页面，沿用后台紧凑工具栏、数据表和固定每页 10 条的分页样式。状态筛选使用“全部、正常、已删除”分段按钮，支持按行程、城市或所属用户搜索。已删除行程显示删除时间，并提供恢复图标；彻底删除使用危险色垃圾桶图标，存在分享、反馈、修订、Agent 运行、规划任务或收藏关联时保持禁用并通过悬停提示原因。用户端垃圾桶操作改为“移入回收站”，执行前显示可恢复确认提示。

### 17.28 会话批量管理

2026-09-02 旅行规划工作台的左侧会话栏新增批量选择模式。使用列表勾选图标进入模式，工具条提供“全选 / 取消全选”、已选数量、归档（归档视图中为恢复）和删除图标操作；单条会话在选择模式中显示 16px 方形勾选状态，不再展开更多操作菜单。归档、恢复和删除统一在居中确认弹窗中执行：弹窗沿用 `--radius-card`、`--color-surface`、`--color-border` 和 `#000000` 50% 遮罩，删除主操作使用危险色。一次删除 3 条及以上时显示 `48px` 高的密码输入框，使用 `type="password"` 避免明文显示。

### 17.29 首页目的地搜索

2026-09-03 首页首屏搜索框支持城市和景点两类结果。输入框保持原有 `64px` 高的圆角搜索形态，结果以紧贴输入框下方的单一浮层展示，不增加嵌套卡片。每个结果行使用定位或指南针图标、主名称、类别与简要地点信息，以及右箭头；行高由 `12px` 垂直内边距决定，悬停和键盘焦点使用 `--color-surface-soft`。浮层沿用 `--radius-card`、`--color-border` 和唯一的 `--shadow-hover`，无结果和加载状态使用 `12px` 辅助文字。移动端保持与输入框等宽，文本超出时省略，不产生横向滚动。

### 17.30 认证 HTML 邮件

2026-09-03 认证邮件使用兼容主流邮件客户端的表格布局，外层为 `#f7f7f7`，内容区域为最大 `600px` 的白色 `14px` 圆角面板，并使用 `#dddddd` 和 `#ebebeb` 分隔内容。品牌文字、主要按钮和验证码强调色使用 Rausch `#ff385c`；标题为 `22px / 600` 的 `#222222`，正文为 `16px`，辅助和安全说明为 `13px ~ 14px` 的 `#6a6a6a`。验证码在独立浅灰区域居中展示，使用 `32px / 700` 和 `8px` 字距；密码重置与邮箱确认邮件使用 `48px` 高、`8px` 圆角的主按钮，并始终提供可复制的备用链接。邮件同时保留纯文本版本，供不支持 HTML 的客户端使用。

### 17.31 邮件投递记录后台

2026-09-03 管理后台的“邮件投递记录”沿用现有紧凑表格，不将邮件内容做成预览卡片。筛选项仅使用全部、已发送、失败和本地模拟四种状态；每行展示用途、用户、脱敏邮箱、状态、尝试/重试次数、时间和错误码。状态以现有语义色区分，错误码使用等宽文字并在列宽不足时省略，避免泄露认证邮件中的验证码、令牌、完整收件人或正文。页面顶部使用常规模块标题与刷新图标按钮，移动端表格允许横向查看，不压缩关键状态字段。
